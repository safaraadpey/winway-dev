/**
 * Future engine placeholder — register scenarios here, then:
 *   registerEngine("backgammon", backgammonScenarios) in src/run.mjs
 *
 * @type {import('../../framework/types.mjs').Scenario[]}
 */
export const backgammonScenarios = [];
