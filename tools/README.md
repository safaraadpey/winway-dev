# Tools

Operator and developer tooling (env sync scripts remain under `scripts/` for ops; ad-hoc helpers under `tools/adhoc`, `tools/dev`, notebooks, etc.).

See `docs/architecture/p4-1-root-cleanup.md` and `docs/architecture/p4-4-workspace-foundation.md`.
