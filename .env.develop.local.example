# Supabase develop branch (staging) — copy to .env.develop.local
# Project ref: ovclbgxtpxyzlcmwbviw
# Keys: Supabase Dashboard → develop branch → Settings → API
# Or: scripts/sync-supabase-develop-env.ps1

NEXT_PUBLIC_ADMIN_ORIGIN=http://localhost:3000
NEXT_PUBLIC_ACTIVE_GAMES_SOURCE=orchestrator
NEXT_PUBLIC_SUPABASE_URL=https://ovclbgxtpxyzlcmwbviw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_develop_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_develop_service_role_key

# Direct Postgres (temporary active_cards debug) — Supabase Dashboard → Database → Connection string → URI
# DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# Shared staging Redis (optional) — for Railway apps/engines/bingo coordination only,
# not Next.js lobby caching. Prefer configuring on the engine service.
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
