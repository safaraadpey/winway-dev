# Infrastructure (placeholder)

Reserved for permanent homes of:

- SQL migrations (`sql/` today — not moved in P4.4)
- Supabase CLI config (`supabase/` today — not moved in P4.4)
- Deploy/cron assets

Do not relocate production SQL in this phase.

See `docs/architecture/p4-4-workspace-foundation.md`.
