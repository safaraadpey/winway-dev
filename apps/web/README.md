# Web app (placeholder path)

The Next.js player/admin application currently lives at the **repository root** (`app/`, `package.json`, `middleware.ts`).

This directory is reserved for the future move:

```
apps/web/   ← target home for the Next.js app (not moved in P4.4)
```

See `docs/architecture/p4-4-workspace-foundation.md`.
